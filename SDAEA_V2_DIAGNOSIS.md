# SDAEA v1 训练日志诊断（作为 v2 对照基线）

## 1. 范围与数据来源

本诊断只描述已经记录和复算确认的事实，不把尚未实施的方法写成结果。

- 运行目录：`runs/sdaea_online_20260723_230645/`
- 逐步日志：`metrics.csv`，共 88,542 行，`step=1..88542`
- 运行配置：`config.json`
- 汇总：`summary.json`
- 三段统计：按步数等分为前、中、后三段，每段 29,514 步
- 本地 Godot 项目检查确认：机器人初始 HP 为 6、最大 HP 为 100；红块造成 -5 HP，绿块造成 +5 HP；当前 reset 控制器只复活死亡机器人，不重置红绿方块世界。

除特别说明外，HP 事件均按 `next_hp_reported - hp` 复算。不能直接用日志中的 `hp_delta` 统计终止步事件，因为终止步的 `next_hp` 会被置为 0。

## 2. 总结判断

这次 v1 运行不能证明策略已经学会延长寿命。

- 策略熵从头到尾几乎等于 27 个联合动作的最大熵，动作频率也接近均匀随机。
- 完成寿命随 episode 顺序没有单调改善，Spearman 相关为 -0.0267。
- 后段每千步死亡数下降，但完成寿命的后段中位数没有提高；结果同时受起始 HP、随机绿块、持续存在的方块世界和少数超长 episode 强烈混杂。
- world model 对普通微量掉血的预测在改善，但没有学会极稀有的红块、绿块和死亡跳变。
- 5 个 episode 在第一步观测时 HP 已经为负，不能把这些死亡归因于该步新选择的动作。

因此，v1 应完整保留为可复核对照，但不应把其均值寿命、后段死亡率或总体 world loss 的下降单独解释为策略学习成功。

## 3. 寿命：均值被长尾样本显著影响

日志包含 41 个完成死亡的 episode，另有第 42 个仍存活、属于右删失样本。

| 统计量 | 完成的 41 个 episode |
| --- | ---: |
| 均值 | 2,073.95 步 |
| 中位数 | 1,161 步 |
| P25 / P75 | 355 / 1,585 步 |
| P90 | 5,884 步 |
| 最大值 | 14,347 步 |
| 标准差 | 3,338.74 步 |

按完成 episode 的时间顺序分为 1–14、15–27、28–41 三组：

| episode 组 | 均值 | 中位数 |
| --- | ---: | ---: |
| 前组 | 1,634.14 | 1,012.5 |
| 中组 | 2,452.00 | 1,283 |
| 后组 | 2,162.71 | 934 |

后组中位数低于前组和中组。另一种直观对比也显示均值提升主要来自长尾：

- 前 10 个完成 episode：均值 1,400.7，中位数 1,225。
- 后 10 个完成 episode：均值 2,830.1，中位数 1,249.5。
- 后 10 个的中位数只增加 24.5 步，而其中包含 14,347 步和 7,782 步的长寿样本，明显抬高了均值。

完成寿命与 episode 序号的 Spearman 相关为 -0.0267，未显示随训练推进而改善的单调趋势。

按等步数窗口统计，前、中、后三段死亡数为 17、15、9，即每千步约 0.576、0.508、0.305 次。这个比率不能单独证明策略变好：长寿 episode 会天然占据更多后段步数，且不同 episode 的起始 HP 并不一致。

## 4. 起始状态与 reset 异常

42 个 episode（含右删失 episode）的第一条 HP 可分为：

| 起始 HP 范围 | episode 数 |
| --- | ---: |
| HP <= 0 | 5 |
| 约 1 | 12 |
| 约 6 | 22 |
| 约 11 | 3 |

Godot 的配置初始 HP 是 6；日志中约 1 和约 11 的起始值与一次 -5 红块碰撞或 +5 绿块碰撞一致。由于 reset 只复活机器人而不会重置方块世界，episode 起点不是统一、独立的初始条件。

5 个异常 episode（life 1、12、24、30、34）第一步 HP 已为负，分别约为 -4 或 -9，并在 1 步内终止。剔除这 5 个样本后，其余 36 个完成 episode 的均值为 2,361.86，中位数为 1,285。

这些行还揭示了一个日志口径问题：终止步将有效 `next_hp` 置为 0，所以负起始 HP 会在 `hp_delta` 中表现为 +4 或 +9 的假正增益。按 `hp_delta > 0` 会错误得到 19 次正增益；按真实上报差值 `next_hp_reported - hp` 计算，只有 14 次。

`summary.json` 与逐步 CSV 还存在固定的 1 步差异：

- `summary.json` 写入 `steps=88543`，CSV 最大 step 为 88,542。
- `summary.json` 写入当前右删失寿命 3,511，CSV 中 life 42 的最大 `life_step` 为 3,510。

## 5. 策略仍接近均匀随机

三个可学习动作头各有 3 个类别，`shoot` 固定为 0，因此联合动作数为 27，理论最大熵为：

`ln(27) = 3.295836866`

| 等步数窗口 | 平均熵 | 与最大熵的差 | 等效动作数 `exp(H)` |
| --- | ---: | ---: | ---: |
| 前段 | 3.295788 | 0.000048 | 26.9987 |
| 中段 | 3.295798 | 0.000039 | 26.9990 |
| 后段 | 3.295767 | 0.000070 | 26.9981 |

等效动作数始终约为 27，没有形成有意义的动作偏好。总体边缘动作频率也接近 1/3：

| 动作头 | 类别 0 | 类别 1 | 类别 2 |
| --- | ---: | ---: | ---: |
| accelerate_forward | 33.009% | 33.872% | 33.119% |
| accelerate_sideways | 33.590% | 33.232% | 33.179% |
| turn | 33.210% | 33.496% | 33.294% |

`shoot=0` 为 100%。各窗口中任一类别相对 1/3 的最大偏离也只有约 0.84 个百分点。

其他策略更新证据与此一致：

- `selected_log_prob_change` 绝对均值仅约 1.66e-5。
- `actor_update_norm` 前、中、后段均值为 2.09e-6、1.67e-6、1.93e-6。
- `current_action_sign_ok` 前、中、后段约为 81.46%、81.59%、81.41%，没有随训练提升。

环境中确实存在可观测的动作代价差异。排除终止和红绿块跳变后，`forward=1, sideways=1` 的 9,980 步 HP 差中位数为 -0.000278；其余 8 种加速组合的中位数均约为 -0.000833，约为前者的 3 倍。但 v1 的动作分布没有向低消耗组合集中。

## 6. HP、正负信号与裁剪

按 `next_hp_reported - hp` 统计：

- 真实绿块 +5 事件只有 14 次，占 88,542 步的 0.0158%。
- 约 -5 的大幅掉血事件共有 54 次，其中 27 次发生在终止步。
- 前、中、后三段绿块事件为 3、8、3 次；大幅掉血事件为 21、19、14 次。

`pleasure_signal` 的分布高度集中：

- 88,487 个普通存活步精确等于 0.002。
- 41 个死亡步为 0。
- 只有 14 个真实绿块步高于 0.002。

所有 41 个死亡步的 `policy_delta` 都精确等于 -3，并且全部触发 TD clip；普通步没有触发 TD clip。非终止步的 `policy_delta` 均值为 2.77e-6，绝对值中位数为 7.14e-4。因而日志中的 TD clip 率主要是在重复记录死亡发生率，而不是普通 TD 误差逐渐稳定的证据。

裁剪同样集中在稀有事件：

- actor direction clip：72 次，其中 41 次为死亡步。
- world gradient clip：45 次，其中 41 次为死亡步。
- 死亡步 world gradient norm 的分段均值为 120.7、127.9、128.7，均远高于配置的 clip=10。

## 7. world model：普通变化改善，稀有事件失败

必须把普通步和终止步分开看，否则总体均值会掩盖失败模式。

| 指标 | 前段 | 中段 | 后段 |
| --- | ---: | ---: | ---: |
| 非终止 world loss 均值 | 0.002430 | 0.002151 | 0.001271 |
| 终止 world loss 均值 | 30.390 | 31.204 | 36.695 |
| 普通 HP 变化 MAE | 0.00334 | 0.00179 | 0.00104 |
| 绿块 HP 跳变 MAE | 0.479 | 0.499 | 0.500 |
| 红块 HP 跳变 MAE | 0.491 | 0.491 | 0.507 |

普通步的 world loss 和 HP 变化误差在下降，但对归一化目标约为 +/-0.5 的红绿块跳变，误差始终约为 0.5，后段没有改善。具体地：

- 绿块目标约 +0.5，预测均值从 +0.0206 变为 +0.00051、再变为 -0.000035。
- 红块目标约 -0.5，预测均值从 -0.0091 变为 -0.0095、再变为 +0.0066。

模型趋向预测最常见的微量掉血，而不是稀有但决定生存的 +/-5 HP 事件。

死亡预测也没有形成稳定区分：

| 等步数窗口 | 终止步预测死亡概率 | 非终止步预测死亡概率 | 当窗 AUC |
| --- | ---: | ---: | ---: |
| 前段 | 0.0610 | 0.0432 | 0.451 |
| 中段 | 0.0438 | 0.0324 | 0.641 |
| 后段 | 0.0295 | 0.0219 | 0.603 |

终止和非终止样本的预测概率都在下降，终止步区分度没有持续改善。终止 world loss 从前段到后段反而上升约 20.8%。

`latent_loss` 的分段均值从 0.001879 降到 0.0000199，下降约 98.9%。当前 CSV 没有记录 latent 方差、范数或跨样本距离，所以仅凭 loss 下降无法判断是有效预测变好还是表征趋同；这项结果不能作为 world representation 成功的证据。

## 8. 混杂因素与研究限制

以下因素限制了从本次单次运行中作因果结论：

1. 起始 HP 不统一。完成寿命与起始 HP 的 Spearman 相关为 0.579。
2. 绿块极稀有且带来 +5 HP。完成寿命与绿块次数的 Spearman 相关为 0.528，但长寿本身也会增加遇到绿块的机会，因此这个相关不能解释为单向因果。
3. 只有一次连续运行，没有相同条件下的随机策略、冻结策略或多随机种子对照。
4. 当前 reset 不重置红绿方块世界，episode 并非独立同分布；不同 episode 面对的方块状态不同。
5. 当前第 42 个 episode 是右删失样本，不能当作一个已完成寿命与前面的死亡 episode 直接平均。
6. 41 次死亡、14 次绿块和 54 次大幅掉血相对于 88,542 步都非常稀少；按全体步数汇总的平均 loss 会被普通微量掉血主导。
7. `env_reward_ignored` 全部为 0，代码也未将它用于学习。模型还有视觉预测与熵正则的更新，因此不能把行为变化全部归因于 HP/死亡反馈，更不能从 reward 为零推出内驱已经有效。

## 9. v1 对照保留要求

本次 v1 运行应作为不可覆盖的原始基线保留：

- 保留原始 `config.json`、`metrics.csv`、`summary.json` 和 checkpoints，不用 v2 输出覆盖该目录。
- v2 对比时同时报告完成寿命的中位数、均值和长尾分位数；均值不能单独作为成功标准。
- 对比时单独报告起始 HP<=0 的无效 episode、起始 HP 分布、右删失寿命、红绿事件数和稀有事件预测误差。
- 策略是否形成偏好，应同时由动作熵、动作边缘频率和低消耗动作占比验证，不能只看更新范数。
- world model 的普通步、红块、绿块和终止步误差必须分开报告，不能只比较总体 world loss。

这些要求用于保证 v1 与 v2 的结论可复算、可比较；它们本身不构成对尚未实施算法效果的声明。
