## 一种面向端侧在线学习的“内稳态自调节 + 死亡回溯”训练框架：基于 Godot 具身仿真与 MobileNetV3 的方法论论文（未实证验证）

**作者**：<your name>

**仓库与最小实现**：本论文对应实现位于当前仓库，核心参考为 `readme.md` 与 `my_battle_zone_godot_train.ipynb`。

---

## 摘要（明确声明未验证）

本文提出一种以“生存压力”为核心筛选信号、面向端侧持续学习的强化学习训练框架，目标是缓解当前主流范式在符号落地、自主性/内驱力、以及在线持续学习方面的结构性限制。框架由三项关键机制构成：（1）**全脑循环工作空间**：模型输出不仅用于动作，还作为下一时刻输入的一部分（“思维流/内部状态”）；（2）**内稳态自调节**：由模型输出中的特定区域自生成训练信号（loss）与学习率（LR），实现“由体内状态调控可塑性”的在线更新；（3）**死亡回溯与反事实学习**：以 HP 归零为唯一硬性负反馈，触发对缓存参数的反向梯度刻录，以抑制导致死亡的行为模式。

需要强调：**本文仅为理论/方法论与系统框架阐述，尚未完成充分的实证验证，也未提供可对外宣称有效的性能结果**。受算力与训练周期限制，本文不报告任务成功率提升等结论。尽管如此，仓库提供了一个最小可行闭环：基于 Godot 的小型具身仿真环境（双目视觉 `left_eye/right_eye` 与 `hp`）以及可运行的训练脚本/Notebook，用以验证数据流、张量形状、单步更新与死亡回溯机制在工程上能够闭环运行。

**关键词**：具身智能；端侧在线学习；全局工作空间；内稳态；死亡回溯；自生成学习率；Godot

---

## 1 引言（明确声明未验证）

当前大模型与深度强化学习多遵循“离线大规模训练 + 部署推理”的二分模式。在语言与视觉等静态任务中，这种范式可依赖海量数据与集中算力取得强能力；但面向具身智能/AGI 的长期目标，仍面临以下三类核心限制（亦可理解为“训练范式的三位一体局限”）：

- **符号落地缺失（Symbol Grounding Problem）**：模型主要从文本/像素统计相关性中学习，缺乏与物理世界交互的具身经验，因果与物理常识难以自然形成。
- **缺乏自主性与内驱力（Lack of Intrinsic Motivation & Agency）**：目标函数与数据集由外部定义，模型更接近被动拟合器；缺少类似生物体“生存本能”驱动的长期探索与自我规划。
- **端侧在线持续学习受限（Online Learning Constraints / Catastrophic Forgetting）**：训练与推理时空分离，部署端因算力/显存与反向传播成本无法实时更新权重，导致难以像生物一样通过持续试错累积经验。

为回应这些限制，本文提出一种生存驱动的自适应进化训练框架：在具身仿真环境中，以“活着”为核心筛选机制；通过循环工作空间把内部状态写入并带入下一时刻；并让模型自生成学习率与训练信号，实现端侧友好的单步在线更新。

**必须再次声明：本文提出的是理论/方法论框架，尚未完成严格实证验证与对照实验**。本文贡献在于把一套可实现的机制组合（输入/输出张量协议、分区解码规则、单步更新、死亡回溯）以可复现的工程形式明确化，为后续研究者在更强算力、更高保真环境、以及严格实验设计下进行验证、消融与扩展提供基础。

---

## 2 问题设定与设计目标

### 2.1 具身交互设定

考虑一个具身智能体与环境的交互序列。每一步环境返回多模态观测与健康值（HP），智能体输出动作并影响下一步观测。

与标准 RL 的区别在于：本文将“内部思维流”显式写入状态递归，并将“学习率/训练信号”由模型自身输出进行门控。

### 2.2 设计目标

- **端侧资源友好**：尽量避免依赖大 batch、长回放缓存、巨大模型与复杂训练工程；强调单步更新可行。
- **具身闭环**：从“观察者数据学习”转向“行动-感知-内部状态循环”的在线学习。
- **可扩展性**：通过统一的输出画布协议，将动作、内部状态写入、学习率门控、训练信号生成等统一在同一张量空间，便于后续扩展到更复杂的世界模型/规划模块。

---

## 3 最小具身仿真环境（Godot 最小实现）

本仓库最小实现基于 Godot + `godot_rl` 的 `GodotEnv`。

### 3.1 观测

Notebook 中环境对单智能体返回观测字典，关键键包括：

- `left_eye`：RGB 图像（hex 字符串编码），解码后形状为 \(300 \times 320 \times 3\)
- `right_eye`：RGB 图像（hex 字符串编码），解码后形状为 \(300 \times 320 \times 3\)
- `hp`：健康值（可能为 list/标量），在预处理阶段归一化

### 3.2 动作空间（多离散）

Notebook 采用多离散动作：

- `accelerate_forward`: 3
- `accelerate_sideways`: 3
- `turn`: 3
- `shoot`: 2

合计 11 个离散 logit 槽位。

---

## 4 全脑循环工作空间：输入张量协议（状态 + 思维流）

本文采用“输出回灌”构造显式递归：模型输出的一部分（或整体）作为下一步输入的“思维流/内部状态”。该机制对应一种工程化的循环全局工作空间：内部表征不是隐式地藏在 RNN 隐状态里，而是显式写入可视化/可切片的张量画布。

### 4.1 预处理与拼接（来自 Notebook 的张量约定）

对每个时间步 \(t\)：

1. 解码左右眼图像：各为 \(300 \times 320 \times 3\)。
2. 沿高度方向拼接：得到 \(600 \times 320 \times 3\)。
3. 归一化并转为 CHW：得到 \(3 \times 600 \times 320\)。
4. 把 `hp` 归一化为 \([0,1]\)，并嵌入为常数条带：构造 \(3 \times 40 \times 320\) 的 hp embedding。
5. 纵向拼接得到当前观测面板：

\[
\phi(o_t, hp_t) \in \mathbb{R}^{3\times 640\times 320}
\]

6. 取上一时刻输出画布 \(y\_{t-1}\in \mathbb{R}^{3\times 640\times 320}\)，首步置零。
7. 沿宽度方向拼接两块面板，得到模型输入：

\[
x*t = \text{concat}\_w\big(\phi(o_t, hp_t),\ y*{t-1}\big)\in \mathbb{R}^{3\times 640\times 640}
\]

其中，\(y\_{t-1}\) 即“思维流/工作空间写入”。

### 4.2 讨论：为何使用显式画布回灌

- **可解释与可工程化**：内部状态以张量画布形式存在，可被分区读写，便于扩展、可视化与调试。
- **统一接口**：动作读出、学习率门控、训练信号生成等均可通过“从画布的某些区域读出”实现，形成统一协议。

---

## 5 模型结构：MobileNetV3 主干 + 上采样输出画布

最小实现采用 `MobileNetV3-Small` 作为视觉主干（可选 ImageNet 预训练），输入 \(3\times 640\times 640\) 的拼接画布，输出 \(3\times 640\times 320\) 的“工作空间写入画布”。

- **输入**：\(x_t\in \mathbb{R}^{3\times 640\times 640}\)
- **输出**：\(y*t = f*\theta(x_t) \in \mathbb{R}^{3\times 640\times 320}\)

输出画布将被分区解码为：动作 logits、loss 区域、学习率区域；同时整体 \(y_t\) 被用于下一步回灌。

---

## 6 内稳态自调节：从输出画布自生成 loss 与学习率

本文的核心方法论是：把“学习率（可塑性强度）”与“训练信号（loss）”从模型输出中读出，形成一种内稳态自调节闭环。

**强调**：此处给出的 loss/LR 计算方式源于仓库最小实现，属于“可运行的机制示例”，并不代表其在任务上必然有效。其有效性、稳定性与可泛化性需要后续实验验证。

### 6.1 loss 区域（LOSS region）

从输出 \(y_t\in \mathbb{R}^{3\times 640\times 320}\) 的中心裁剪一个 \(3\times 8\times 8\) 区域作为 LOSS region。最小实现使用索引（以中心点 \((320,160)\) 为基准）：

- 高度：`316:324`
- 宽度：`156:164`

记该区域为 \(z_t\)。最小实现的训练信号为：

\[
L_t = \mathbb{E}[|z_t|] - \lambda\,\mathrm{Var}(z_t),\quad \lambda=0.1
\]

直觉上，它为模型提供了一个可微的内部正则/稳态代价，并通过与“思维流回灌”的耦合，有可能诱导出对环境可用的内部表示（但这一点尚未验证）。

### 6.2 学习率区域（LR region）

从 LOSS region 的右侧取相邻 \(3\times 8\times 8\) 区域作为 LR region（最小实现宽度 `164:172`）。对其均值 \(\mu*t\) 做 \(\tanh\) 归一化，映射到学习率区间 \([lr*{min}, lr\_{max}]\)：

\[
\alpha*t = lr*{min} + \frac{\tanh(\mu*t)+1}{2}\,(lr*{max}-lr\_{min})
\]

并在每一步把优化器的学习率设置为 \(\alpha_t\)。这对应“由体内状态调控神经可塑性”的工程化近似。

---

## 7 动作读出：从输出画布分块提取多离散 logits

最小实现把输出画布顶部连续切分为 11 个 \(3\times 8\times 8\) 小块（每块对应一个 logit 槽位），对每块取均值得到标量 logit，形成长度 11 的 logit 向量，再按动作维度切片：

- `accelerate_forward`: 3 logits
- `accelerate_sideways`: 3 logits
- `turn`: 3 logits
- `shoot`: 2 logits

对每个动作维度做 softmax 并采样得到动作。该设计把“动作头”从传统 MLP head 变为“画布分区读出”，与工作空间写入机制保持一致。

---

## 8 训练算法：单步更新 + 参数缓存 + 死亡回溯（反事实更新）

### 8.1 单步在线更新（资源友好）

每与环境交互一步，即执行一次参数更新：

\[
\theta*{t+1} = \theta_t - \alpha_t\,\nabla*\theta L_t
\]

其优势在于：无需大规模 batch 与 replay buffer；中间激活和梯度可快速释放，理论上更适合端侧持续学习的资源约束场景。

### 8.2 参数缓存（parameter cache）

每隔 `cache_every` 步缓存一次模型参数快照 \(\theta^{cache}\)，用于死亡回溯。

### 8.3 死亡回溯与反事实学习（Death-Driven Negative Replay）

当 `hp <= 0`（死亡事件）发生时，最小实现执行：

1. 回滚到最近缓存参数 \(\theta^{cache}\)
2. 用当前状态重算 loss（基于缓存参数）
3. 构造反事实 loss：\(L'\_t=-L_t\)
4. 用较大的 `death_lr` 做一次更新：

\[
\theta \leftarrow \theta^{cache} - \eta\,\nabla*\theta (-L_t) = \theta^{cache} + \eta\,\nabla*\theta L_t
\]

直觉上，这相当于在死亡发生时对某种梯度方向进行“大步长刻录”，以形成对导致死亡的模式的抑制信号。

**强调**：该机制的理论合理性与实际效果尚未验证。其可能带来不稳定、发散或学习到与任务无关的偏置，必须通过对照实验与消融研究来评估。

---

## 9 最小可行验证（工程闭环，而非效果验证）

本文不报告“训练成功/性能提升”的实验结论，但仓库提供了以下最小验证（sanity check），用于证明机制在工程上可闭环：

- **环境接口可用**：Godot 环境返回 `left_eye/right_eye/hp`，并可被正确解码与处理。
- **张量协议可用**：输入画布 \(3\times 640\times 640\) 与输出画布 \(3\times 640\times 320\) 的形状闭环可运行，`out_next` 可回灌。
- **训练闭环可用**：每步 loss 反传与动态学习率更新可执行；参数缓存与死亡回溯逻辑可触发并更新参数。

这些仅证明“方法可运行”，不构成“方法有效”的证据。

---

## 10 局限性与风险（必须后续实证验证）

- **自生成 loss 的目标对齐问题**：当前 loss 形式更像内部稳态正则，并不直接与任务成功相关；是否能诱导出可用行为不确定。
- **死亡回溯的稳定性**：大步长更新可能引发训练不稳定或灾难性偏移；缓存间隔、death_lr 与梯度裁剪策略需系统研究。
- **缺乏严格对照与消融**：尚未验证该框架相对常规 RL（外部奖励、优势函数、回放缓存等）的收益与适用边界。
- **多智能体信用分配**：环境支持多智能体动作格式，但最小实现主要以单体/agent0 为训练对象；协作/对抗下的信号分配尚未讨论。
- **端侧持续学习的遗忘与泛化**：单步更新是否会导致强烈的短期漂移、以及如何在持续学习中保持长期知识，均需进一步机制补充（例如结构化记忆、稳定性约束或元学习）。

---

## 11 未来工作（建议的验证路线）

- **实验设计与指标**：建立可复现的任务集合与评估协议（生存时间、任务达成率、能量/损伤、样本效率、稳定性等）。
- **消融研究**：分别移除（1）out_next 回灌；（2）LR 自调节；（3）死亡回溯；（4）输出画布分块动作头，对比学习曲线与稳定性。
- **改造内部 loss**：将内部 loss 替换为更接近“预测误差/自监督世界模型误差/风险敏感目标”的形式，并测试与生存信号的耦合效果。
- **扩展到更复杂环境**：更高保真物理、更多传感器、更长时间跨度的任务，以检验“仅生存压力能否诱导复杂能力”。

---

## 12 结论

本文提出了一种面向端侧在线学习的生存驱动训练框架：通过循环工作空间把内部状态显式回灌；用输出画布分区实现学习率自调节与自生成训练信号；并以死亡事件触发参数回溯与反事实更新作为唯一硬负反馈。本文**明确尚未完成实证验证**，但提供了基于 Godot 小型具身环境与可运行代码的最小闭环实现，为后续系统实验、机制替换与规模化验证提供了工程基础。

---

## 参考方向（非“已验证结论”，仅为读者背景补充）

- Sutton, R. S., & Barto, A. G. _Reinforcement Learning: An Introduction_.
- Baars, B. J. Global Workspace Theory 相关理论与综述。
- Howard et al. MobileNetV3。
- Godot 与 `godot_rl` 工具链相关文档。

---

## 复现清单（Reproducibility Checklist）

- **代码入口**：`my_battle_zone_godot_train.ipynb`
- **环境**：Godot 编辑器/导出程序 + `godot_rl` 的 `GodotEnv`
- **观测协议**：`left_eye/right_eye` hex 图像 + `hp`
- **输入形状**：\(3\times 640\times 640\)
- **输出形状**：\(3\times 640\times 320\)
- **关键机制**：
  - out_next 回灌（循环工作空间）
  - 输出画布分区：动作 logits / LOSS region / LR region
  - 单步更新（per-step update）
  - 参数缓存（cache_every）
  - 死亡回溯（death_lr + 反事实 loss）
